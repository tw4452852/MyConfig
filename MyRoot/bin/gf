#!/usr/bin/env elvish

var dir
try {
  set dir = (git rev-parse --show-toplevel 2>/dev/null)
  if (==s $dir $E:HOME) {
    set dir = $pwd
  }
} catch {
  set dir = $pwd
}

alacritty --working-directory $dir -e f

